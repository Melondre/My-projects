#include <iostream>

using namespace std;

int main()
{
	double a = 0;
	double b = 0;

	cin >> a >> b;

	cout << "Summa = " << (a * a) + (b * b) << "\n";
	cout << "Rashnost = " << (a * a) - (b * b) << "\n";
	cout << "Proisvedenie = " << (a * a) * (b * b) << "\n";
	cout << "Chastnoe = " << (a * a) / (b * b) << "\n";
}
